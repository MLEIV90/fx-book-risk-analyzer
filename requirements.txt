streamlit>=1.40
# Exact-pinned: these affect numerical output (VaR, GARCH, stats), so a
# future minor/patch version could silently change results. Everything
# else below is UI/IO-only and can float.
numpy==2.5.0
pandas==3.0.3
scipy==1.18.0
arch==8.0.0
matplotlib>=3.8
plotly>=5.20
yfinance>=0.2.40
pandas-datareader>=0.10
xlsxwriter>=3.1
